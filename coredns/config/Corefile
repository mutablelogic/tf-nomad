nomad {
    nomad {
        address {$NOMAD_ADDR}
        token {$NOMAD_TOKEN}
        ttl {$CACHE_TTL}
    }
    cache {$CACHE_TTL}
    errors
    log
}

. {
    forward . /etc/resolv.conf
    errors
}

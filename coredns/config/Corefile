{{ env "DNS_ZONE" }} {
    nomad {
        address {{ env "NOMAD_ADDR" }}
        token {{ env "NOMAD_TOKEN" }}
        ttl {{ env "CACHE_TTL" }}
    }
    cache {{ env "CACHE_TTL" }}
    errors
    log
}

. {
    forward . /etc/resolv.conf
    errors
}
